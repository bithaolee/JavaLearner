#laravel学习
-------

##1. laravel5的目录结构详细分析

	+--app
	|
	|--bootstrap
	|
	|--config
	|
	|--database
	|
	|--public
	|
	|--resources
	|
	|--storage
	|
	|--test
	|
	|--vendor
		 |
		 |--...其它包
		 |
		 |--laravel（laravel框架目录）
			   |
			   |--framework
					|
					|--src
						|
						|--Illuminate
							 |
							 |--Contracts(接口集合)
							 |
							 |--Foundation(框架初始化核心代码)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)
							 |
							 |--Contracts(接口集合)